# 降级策略是用户维度的

Fallback（跨模型降级）策略绑定到用户，而非模型。不同用户请求同一个模型时，可以有不同的降级链。

原有设计中 fallbacks 是全局配置，所有用户共享同一降级策略。但实际上不同用户有不同的服务等级——免费用户 gpt-4 失败后降级到 gpt-3.5-turbo，付费用户降级到 gpt-4-turbo。

新设计：
- 全局默认降级策略定义在 YAML `fallbacks` 段（运维层面兜底）
- 用户级降级策略存在数据库 `user_model_fallbacks` 表（业务层面个性化）
- 数据库记录优先于 YAML 配置
